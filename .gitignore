__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/
venv/

# segredos e credenciais: nunca versionar (o repositório é público)
.env
.env.*
*.pem
*.key
*.pfx
*.p12
.azure/

# teste no navegador: dependências e fotos de tela
tests/navegador/node_modules/
tests/navegador/fotos/

# PDFs novos ficam fora do git: a base de legislação vem deles (scripts/construir_base_de_fontes.py),
# e o que vai para o repositório é o texto extraído (dados/fontes/). Alguns PDFs são edições comerciais.
*.pdf
# exceção: os testes leem as 10 sentenças deste PDF (pacote sentencas/), inclusive no GitHub Actions
!Conjunto de Treinamento - 10 Sentenças Judiciais.pdf

# configuração local do editor/assistente
.claude/

# diário de trabalho local (decisões e próximos passos), fora do repositório público
docs/progresso.md

# dicionários de terceiros baixados para gerar o vocabulário do agente (scripts/construir_vocabulario.py)
dicionarios/
